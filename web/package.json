{
  "name": "briefcase-descent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Hyper-lightweight low-poly faller engine + game (runs in browser and pywebview).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "ogl": "^1.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
